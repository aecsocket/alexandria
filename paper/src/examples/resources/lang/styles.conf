# Defines the "tone" of the plugin.
accent: { color: "#aabbcc" }
# Generic information.
info: { color: "gray" }
# Variable information.
var: { color: "white" }
# Error messages.
error: { color: "red" }
# A key in a key/value pair.
key: { color: "dark_green" }
